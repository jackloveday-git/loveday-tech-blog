{{! Post Info Partial by Jack Loveday}}

{{! Display Current Post info}}
<article class="post">
    <div class="title">
        {{! Show post title and author}}
        <span><a href="/post/{{id}}">{{title}}</a></span><span style="float:right">Posted by:
            {{user.username}}</span><br>
        {{! Style the comment to float right for cleaner UI}}
        <span style="float:right">{{comments.length}} {{format_plural "comment" comments.length}}</span>
    </div>
    <div class="content">
        <p>{{post_text}}</p>
    </div>
</article>